<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, minimum-scale=1, user-scalable=yes">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Holtwood+One+SC&family=Inter:wght@400;500;600;700&family=Manrope:wght@200;300;400;500;600;700;800&family=Noto+Sans+JP:wght@400;500;600&family=Outfit:wght@700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="{{ 'simple-notify.min.css' | asset_url }}" />
    <script src="{{ 'simple-notify.min.js' | asset_url }}"></script>

    <style>
      :root {
        --linear-gradient: linear-gradient(165deg, {{ settings.gradient_color_1 }} 0%, {{ settings.gradient_color_2 }} 42.19%, {{ settings.gradient_color_3 }} 100%);
      }
    </style>

    <link href="{{ 'index.css'  | asset_url }}" rel="stylesheet" />

    {{ content_for_header }}
  </head>
  <body>
    <script src="{{ 'index.js' | asset_url }}" type="module"></script>

    {{ content_for_layout }}
  </body>
</html>